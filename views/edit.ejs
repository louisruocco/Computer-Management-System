<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/views/css/styles.css">
    <title>CMS | Edit <%= device[0].name %></title>
</head>
<body>
    <div class="link">
        <a href="/home/<%= device[0].name %>">Back to Workstation</a>
   </div>
    <div class="title">
        <h1>Edit <%= device[0].name %></h1>
    </div>
    <div class="add-div">
        <form action="/auth/edit/<%= device[0].name %>" method = "POST">
            <div class="name-section">
                <input type="text" name = "name" value = "<%= device[0].name %>">
            </div>
            <div class="os-section">
                <input type="text" name = "os" value = "<%= device[0].os %>">
            </div>
            <div class="spec-section">
                <textarea name="spec" cols="30" rows="10"><%= device[0].spec %></textarea>
            </div>
            <div class="Storage-section">
                <textarea name="storage" cols="30" rows="10"><%= device[0].storage %></textarea>
            </div>
            <div class="button">
                <button>Update</button>
            </div>
        </form>
    </div>
</body>
</html>